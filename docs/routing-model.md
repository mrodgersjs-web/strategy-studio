# Routing model

```text
Question
  → Archetype (A1–A4)
  → Cell id
  → Required inputs
  → Gates
  → Decision record + proof
```

Cells are deterministic checklists first; LLMs fill transformations only inside the cell.
